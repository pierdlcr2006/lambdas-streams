package stream;

import java.util.Arrays;
import java.util.List;

public class StreamConcept {
    //Flujo de datos, se itrea con el flujo de dato y no con bucles
    //Operadores nos ayudan a trabajar con la información, operadores finales y no finales
    //Operadores finales, terminará el flujo, después de ese operador no se hace nada (forEach)
    //Operadores no finales, después de operar con el se continuará el flujo
    public static void main(String[] args) {
        List<String> names = Arrays.asList("Ana", "Bob", "Charlie","Dan", "Elijah", "Fred", "George");
    }
}
